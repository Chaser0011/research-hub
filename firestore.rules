rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allows any signed-in user to read and write any data
      allow read, write: if request.auth != null;
    }
  }
}